# Phase 1

Runtime/configuration preparation is complete.

- Node 22 is the target runtime.
- Environment variables have a documented template.
- Runtime secrets are no longer intended to live in source configuration.
- Environment validation is available through `node scripts/validate-env.js`.

The Discord.js/Erela API upgrade is intentionally deferred until this runtime baseline is verified.
